# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# public/api, public/status and public/calendar.ics used to be ignored, back
# when Netlify Functions generated them at runtime. They are now generated by
# the nightly GitHub Action and committed, because they are what the static
# site actually serves.

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
